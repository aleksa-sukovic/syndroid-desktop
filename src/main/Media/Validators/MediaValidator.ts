import BaseValidator from "../../Library/Validators/BaseValidator";
import Rule from "../../Library/Validators/Rule";

export default class MediaValidator extends BaseValidator
{
    public getRules(): Rule[]
    {
        return [
            //
        ];
    }
}
