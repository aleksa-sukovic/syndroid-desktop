import Rule from "../../Library/Validators/Rule";
import LoggedValidator from "../../Library/Validators/LoggedValidator";

export default class MediaValidator extends LoggedValidator
{
    public getRules(): Rule[]
    {
        return [
            //
        ];
    }
}
